class IncludedModel {
  IncludedModel({
    this.id,
    this.type,
    this.name,
    this.categoryType,
    this.description,
    this.slug,
    this.isApproved,
  });

  String? id, type, name, categoryType, description, slug;
  bool? isApproved;
}
